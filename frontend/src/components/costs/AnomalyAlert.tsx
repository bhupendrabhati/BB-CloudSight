import React from 'react';
import { AlertTriangle } from 'lucide-react';

interface AnomalyAlertProps {
  accountId?: string;
}

export default function AnomalyAlert({ accountId }: AnomalyAlertProps) {
  return null;
}
